// Normal Function
/* function sum(a, b){
  return a+ b;
} */

  // Arrow Function
const sum = (a,b) => {
  return a + b
}

// If  We've only one line return functions use the below methods
// const sums = (a, b) => a + b;
module.exports = {sum}